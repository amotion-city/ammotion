<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>Hello Ammo!</title>
    <!-- link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet" -->
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <nav role="navigation">
          <ul class="nav nav-pills pull-right">
            <li><a href="http://www.phoenixframework.org/docs">Get Started</a></li>
          </ul>

          <div class="identity">
            <%= if @current_user do %>
              <div class="loggeduser">
                <img alt="<%= @current_user.name %>"
                    class="circle responsive-img"
                    src="<%= @current_user.avatar %>"/>
                <%= button "🚪", to: auth_path(@conn, :delete), method: :delete, class: "btn btn-floating btn-danger" %>
              </div>
            <% else %>
              <a class="btn btn-floating pulse" href="/auth/github">
                <i class="fab fa-github"></i>
              </a>

              <a class="btn btn-floating pulse" href="/auth/facebook">
                <i class="fab fa-facebook"></i>
              </a>

              <a class="btn btn-floating pulse" href="/auth/google">
                <i class="fab fa-google"></i>
              </a>

              <a class="btn btn-floating pulse" href="/auth/slack">
                <i class="fab fa-slack"></i>
              </a>

              <a class="btn btn-floating pulse" href="/auth/twitter">
                <i class="fab fa-twitter"></i>
              </a>

              <a class="btn btn-floating pulse" href="/auth/identity">
                <i class="fas fa-user"></i>
              </a>
            <% end %>
          </div>
        </nav>

        <span class="logo"></span>
      </header>

      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>

    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
